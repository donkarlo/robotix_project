from enum import auto


class PreDictionary:
    mission=auto()
    plan=auto()
    action=auto()
    vison=auto()
    #from coclea
    orientation=auto()
    # only relative to other_kind objects
    position = auto()
