from robotix.structure.kind.body.sensor.kind.lidar.observation.decorator.decorator import Decorator


class Pointed(Decorator):
    """
    For the time that we
    """
    def __init__(self):
        pass