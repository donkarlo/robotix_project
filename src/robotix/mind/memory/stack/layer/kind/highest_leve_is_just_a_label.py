from robotix.mind.memory.stack.layer.layer import Layer


class HighestLeveIsJustALabel(Layer):
    """
    The highest lower_layer of all experiences must be just a name or a name  which is current we set as experience name
    """
    pass